# @openfolio/hosted

Convex-hosted services for OpenFolio paid capabilities:

- account and device auth
- billing entitlements
- managed AI relay
- managed connectors

This package intentionally does not store raw Messages archives or sync the entire local graph by default.
